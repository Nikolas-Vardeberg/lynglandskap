/** @type {import('next-sitemap').IConfig} */

module.exports = {
    siteUrl: process.env.SITE_URL || 'https://www.lynglandskap.no/',
    generateRobotsTxt: true, // (optional) Generate robots.txt
    // Optional additional options
  };
  